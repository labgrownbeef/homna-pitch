# 3.1 Core Components

**3.1.1 Market Data Processor**

Processes structured financial data from various sources:

```python
class MarketDataProcessor:
    def __init__(self, config):
        self.config = config
        self.data_sources = self._initialize_data_sources()
        
    def process_market_data(self, ticker, start_date, end_date):
        """Process market data for a specific ticker and time range."""
        # Implementation details
        
    def extract_technical_indicators(self, price_data):
        """Extract technical indicators from price data."""
        # Implementation details
```

**3.1.2 News and Social Media Analyzer**

Processes unstructured text data from news and social media:

```python
class SentimentAnalyzer:
    def __init__(self, model_path):
        self.model = self._load_model(model_path)
        self.tokenizer = self._load_tokenizer()
        
    def analyze_sentiment(self, text):
        """Analyze sentiment of financial text."""
        # Implementation details
        
    def extract_entities(self, text):
        """Extract relevant entities from text."""
        # Implementation details
```

**3.1.3 Memory Manager**

Manages the different memory layers and their interactions:

```python
class MemoryManager:
    def __init__(self, config):
        self.config = config
        self.working_memory = WorkingMemory(config)
        self.short_term_memory = ShortTermMemory(config)
        self.long_term_memory = LongTermMemory(config)
        self.permanent_memory = PermanentMemory(config)
        
    def store(self, information, memory_type):
        """Store information in the appropriate memory layer."""
        # Implementation details
        
    def retrieve(self, query, top_k=5):
        """Retrieve information from memory based on query."""
        # Implementation details
        
    def consolidate_memories(self):
        """Consolidate memories between different layers."""
        # Implementation details
```

**3.1.4 Decision Support Engine**

Integrates memory contents with current market state to generate recommendations:

```python
class DecisionSupportEngine:
    def __init__(self, memory_manager, market_data_processor):
        self.memory_manager = memory_manager
        self.market_data_processor = market_data_processor
        self.llm = self._initialize_llm()
        
    def generate_recommendation(self, ticker, current_market_state):
        """Generate trading recommendation for a specific ticker."""
        # Implementation details
        
    def explain_recommendation(self, recommendation_id):
        """Provide explanation for a generated recommendation."""
        # Implementation details
```
