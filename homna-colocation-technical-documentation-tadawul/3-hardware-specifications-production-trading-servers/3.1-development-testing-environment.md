# 3.1 Development/Testing Environment

**VM Configuration (3x virtual machines)**

```yaml
Operating_System:
  distribution: "Ubuntu 22.04 LTS"
  kernel: "5.15.0 with RT patches"

Resources:
  cpu_cores: 4
  memory: "32GB"
  storage: "500GB SSD"

Network_Access:
  market_data:
    - ITCH Total View (read-only)
    - ITCH News
    - GLIMPSE
  order_routing: "Simulation gateway only"
```

#### 3.3 FPGA Acceleration (Optional Phase 2)

```yaml
FPGA_Card:
  model: "Xilinx Alveo U250"
  capabilities:
    - Market data parsing
    - Order book construction
    - Simple strategy logic
  expected_latency: "300-800 nanoseconds"
```
